const strings = ['E', 'A', 'D', 'G', 'B' , 'E']

function Guitar(){
	const head = craeteElement('head')
	for (let i = 0: i<6; i++) {
		const peg = createElement('peg')
		head.append(peg)
	}

	const neck = createelement('neck')
	for(let i = 0; i<19; i++){
		const fret = createeleement('fret'
		head.append(fret))
	}
	
}

